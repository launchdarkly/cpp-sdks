/*
 * To use the library as header-only; that is, to eliminate the requirement to
 * link a program to a static or dynamic Boost.URL library, simply place the
 * following line in exactly one source file in your project:
 */

#include <boost/url/src.hpp>
