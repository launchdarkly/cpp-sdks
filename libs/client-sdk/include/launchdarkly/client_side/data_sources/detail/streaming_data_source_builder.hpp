#pragma once

#include "config/detail/service_endpoints.hpp"

class StreamingDataSourceBuilder {
    StreamingDataSourceBuilder& endpoints(
        launchdarkly::config::ServiceEndpoints endpoints);

    StreamingData


};